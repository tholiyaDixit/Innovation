/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        // domains: ["https://picsum.photos/200"],
        // domains: ["https://nextjs.org/docs/messages/next-image-unconfigured-host"],
      },
};

export default nextConfig;
